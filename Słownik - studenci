"""
zadanie 5:
# napisz program który poprosi o wprowadzenie imienia studenta oraz oceny jaką uzyskał,
"x" kończy wprowadzanie danych,
po zakończeniu wprowadzania danych zwraca średnią ocen dla każdego studenta oraz dla całej klasy
[słowniki do wykorzystania]
"""

#WORK IN PROGRESS

"""
def oceny_studenta():
    lista_ocen = []
    lista_list = [lista_ocen] #nie wejde w tym niepotrzebnie w tablice 2-wymiarowa?
    imie = str(input("Wprowadź imię studenta"))
    while True:
        oceny = int(input("Jakie oceny uzyskał student?"))
        lista_ocen.append(oceny)
        print(lista_ocen)
        ucznowie_oceny[imie] = oceny
        print(ucznowie_oceny)
        pytanie = str(input("Chcesz dalej wprowadzać oceny? Y/N"))
        print(lista_list)
        if pytanie == "N" or pytanie == "n":
            pytanie2 = str(input("Kolejny uczeń? Y/N"))
            if pytanie2 == "Y" or pytanie2 == "y":
                oceny_studenta()
            else:
                break
    print("Dzięki")

ucznowie_oceny = {}
oceny_studenta()
"""

#BRUDNOPIS
"""
#do jednego ucznia mozna przypisac wiecej ocen, dlatego trzeba to uwzglednic w petli
#jak przypisac wiele wartosci do jednego klucza w slowniku? lista! tylko jeszcze wykombinowac jak to zrobic i bedzie dobrze
"""
oceny_studentow = {}
x = str(input("Podaj imię"))
y = int(input("Podaj oceny"))
oceny_studentow["Imie"] = x
oceny_studentow["Oceny"] = [y]
print(oceny_studentow)
print(type(y))"""
